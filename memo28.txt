メモ
	git add
		ステージに追加
	git commit
		ステージにあるものをローカルリポジトリに反映
	git diff
		git addしていない変更を表示
	git diff --staged
		git addしたけど未コミットの変更を表示

	git checkout HEAD ファイル名
		変更を取り消し※HEADは最新のコミットの状態に戻すことを意味する

	git pull <remote_ref> <branchname>
	※git pull origin master
	
	git push <remote_ref> <branchname>
	※git push origin master
	★pushする前にpullすること
		ローカルリポジトリをリモートリポジトリに反映
	
	git config --get remote.origin.url
	git remote -v
		URLの取得


# 28 GitHubの画面を確認しよう
	Rawとは
		実際のデータ
	Blameとは
		いつどんな修正が行われたか

2026.08.23
# 29 コマンドにエイリアスを付けよう
	git config --global alias.ci commit
	git config --global alias.st status
	git config --global alias.br branch
	git config --global alias.co checkout
		※--globalを付けることで、現在のプロジェクトだけではなく全てのプロジェクトが対象となる。

2026.08.23
# 30 バージョン管理しないファイルは無視しよう
	.gitignoreファイルに指定する
		.gitignore
			# 指定したファイルを除外
			index.html
			# ルートディレクトリを指定
			/root.html

2026.08.23
# 31 変更を元に戻そう
	git checkout -- ファイル名
	git checkout -- ディレクトリ名
	# 全変更を取り消す
	git checkout -- .

2026.09.02
# 32 ステージした変更を取り消そう
	git reset HEAD index.html

	例）
		git add memo28.txt
		git status
			コミットしていないファイルがあるよ
		git reset memo28.txt
	git 	git status
			ステージに追加していないファイルがあるよ

2026.09.22
# 33 直前のコミットをやり直そう
	git commit --amend
		※リモートリポジトリにpushしたものに対しての使用はNG
		→git revert <コミットiD>でコミットを打ち消す新しいコミットを作成する。

